# Thunderegg Backlog (lego-loop)

Loop implements top unblocked item only. `[proposed]` items need James's promotion (remove tag). See `~/Projects/lego-loop/LOOP.md` for rules.

**Sandbox proof capability (verified 2026-06-26, run 11).** From the Cowork Linux sandbox, only `tsc --noEmit` runs (pure JS — exit 0). `npm run build` (esbuild) and `npx vitest run` (vitest 4 pulls in `rolldown`) both need a host-native binary (`@esbuild/linux-*`, `@rolldown/binding-linux-*`) that isn't in the shared macOS `node_modules`, so both fail on Linux. Consequence: a **type-only** change is sandbox-provable; anything touching the build output or tests must be proven on **macOS (CC-on-Tau)**. See `lego-loop/reports/2026-06-26.md`.

1. **Test harness** — vitest `^4.1.8` + `"test": "vitest run"` in place on `master` with `tests/core.test.ts` (16 cases), `tests/grade-meta.test.ts` (run 15), and `tests/core-edges.test.ts` (**run 17, `223342f`** — shellQuote global-escape + empty-arg, buildBondGraph target-out-of-root/self-link/empty-targets, isConvertible bare-ext contract, isCondenser threshold-0). Also `tests/publish-parse-edges.test.ts` (**run 19, `2071ad2`** — the sidecar/lineage frontmatter parsers' all-three-or-null contract: `parseSidecarSignature` + `parseLineageFrontmatter` reject partial blocks, and `signature_algo:` is not mistaken for the bare `signature` field; hardens the parse-back surface added by `44c147b`/`d23a6df`/`3aab144`). Also `tests/publish-fork-sanitize.test.ts` (**run 21, `1c177b8`** — `sanitizeForkArtifact`'s untrusted-field defaulting at the fork-import boundary: `visibility` falls back to the privacy-safe `private` on garbage/wrong-type/absent (valid values preserved), `license`→`unknown` when empty, `topics` string-filtered (non-array→[]), `schema` forced to `distill.map/0.2` over a forged input, and `provenance` rebuilt keeping only known fields; complements the kinds/ai_assisted/forked_from coverage already in `publish-fork.test.ts`). Also `tests/redaction-scan.test.ts` (**run 23, `99ea9c4`** — the pre-publish privacy gate `redactionScan`: a blocked `#tag` in the *title* (a distinct scanned source from the node-body case), `#`-agnostic case-insensitive zone matching via `normTag`, PII detected in *metadata* (summary + provenance `source_title`, each with its own `where` label), and the two-tier contract — a zone hit is a HARD `block`, PII is a SOFT `warning`, the two independent; complements the topic/node-tag/node-PII coverage already in `publish-core.test.ts`). Also `tests/validate-artifact.test.ts` (**run 25, `9d8486b`** — the defense-in-depth structural re-check `validateArtifact`: the `map.format` mismatch branch incl. the `String(undefined)` coercion when `format` is absent, the per-node `NODE_TEXT_CAP` overflow named by node id at the exact 280 boundary (contract is `>` not `>=`, so 280 passes / 281 fails), a message-pinned schema mismatch, and defense-in-depth accumulation — independent structural + delegated `validatePublishMeta` errors all surface together with no short-circuit; the prior `validateArtifact` block only asserted good→[] and one generic bad→>0). Also `tests/publish-sign-verify-edges.test.ts` (**run 27, `6771922`** — the Ed25519 authorship trust boundary in `publish-sign.ts` (the file's own header calls signing "the moat"): isolates the rejection reasons `publish-sign.test.ts` blurs together — an **empty signature against a *valid* key** (the existing `verifyBytes("x","","not-a-key")` conflates empty-sig with bad-key), a **wrong-length (63-byte) signature** rejected without throwing, and **mismatched (data, signature) pairs cross-checked both directions** (the artifact-substitution threat, distinct from the existing 1-byte tamper case); pins the `publicKeySpki` **SPKI-DER interop round-trip** the sidecar public key rides on (44-byte Ed25519 SPKI, re-import→re-export identical), the `keyFingerprint(spki) === contentHash(spki).slice(0,16)` lockstep contract, and the doc-comment's "exact UTF-8 bytes" promise (publish-sign.ts:8,28) via an **NFC-signed / NFD-presented rejection**; complements the happy-path/tamper/wrong-key/garbage coverage already in `publish-sign.test.ts`). Edge cases proven in-sandbox (tsc --noEmit clean + 21 `node` assertions vs the real compiled `core.ts`); the vitest run itself is macOS-only (capability note). Remaining: coverage of the actual distill/summarize **conversion** path lives in `main.ts` (Obsidian-coupled) and needs extraction first — see #5. *Scope: land (phase-1 — test infra).*
2. **Pre-land gate** — ✅ **LANDED on `master` (run 13, `1b1066b`)** as a host-aware `scripts/preland-gate.sh`: staged-secret guard (`.env`/`data.json`/`*.pem`/`*.key`) + `tsc --noEmit` on every platform; `npm run build` + `npm test` (vitest) run on **macOS only** (esbuild/rolldown native bindings are absent in the Linux sandbox). Proven green in-sandbox against the master tree; guard verified to fail on a staged `.env`/`.key` and to pass the committed `main.js`. Remaining (CC-on-Tau): run it once on macOS to confirm the build+test legs are green. *Scope: land (phase-1 — done).*

3. **TESTING.md → executable** — ✅ **LANDED on `master` (run 15, `c34ec73`)**: `tests/grade-meta.test.ts` converts TESTING.md §4 "Grade badges" into assertions on `GRADE_META` (exact label/icon/css for vapor/distillate/essence) plus the `VALID_GRADES`⇄`GRADE_META` parity the badge renderer relies on. Proven in-sandbox: `tsc --noEmit` clean + the assertions executed green under `node` (vitest runs the same on macOS via the gate). Remaining TESTING.md steps (status bar, file/clipboard conversion, settings UI) are Obsidian-runtime and need extraction first — see #5. *Scope: land (phase-1 — test coverage).*
4. [proposed] Split `main.ts` into modules (only after tests exist to catch regressions).
5. [proposed] **Extract remaining TESTING.md-pure logic into `core.ts`** so it is unit-testable: output filename (`<name>.<ext>.md`), clipboard note name (`Clipboard <timestamp>.md`) + temp-file (`_thunderegg_clip_*`) cleanup pattern, condenser-threshold clamp (range 2–30, default 5), and `DEFAULT_SETTINGS` defaults — all currently in `main.ts` (Obsidian-coupled). Extraction touches product code and needs `npm run build` + vitest, so it is macOS-proof only: branch + Decision Brief for CC-on-Tau, not a sandbox auto-land. (proposed run 15)

## Fable pass adoptions (2026-07-03) — for the lego-loop
- [ ] **Share-card renderer** (fable-pass #9): every export also writes `<title>.thunderegg.png` — map drawn at preserved x/y/w/h geometry on an offscreen HTMLCanvasElement, footer = title + node/edge/source counts + author key fingerprint + wordmark constant (single text const; neutral until trademark knockout returns). Scope strictly: geometry-verbatim render + one footer design. Funnel link goes in the export Notice + fork-note template FIRST (independent of the card).
- [ ] **"My Collection" local changelog** (fable-pass #10): command that scans `Thunderegg Exports/` + provenance sidecars → regenerates ONE Markdown note (timeline of maps, unique source_keys, fork lineage in/out, key fingerprint). Optional "copy as public page" static HTML. Anti-vanity cap: one note, no charts, no streaks. Private exports NEVER count toward demand-gate metrics.
